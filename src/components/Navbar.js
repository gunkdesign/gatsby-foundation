import React from 'react'
import {Link} from 'gatsby'

const Navbar = () => {
    return (
        <nav>
            <ul className="menu">
            <Link to="/">One</Link>
            </ul>
        </nav>
    )
}

export default Navbar
